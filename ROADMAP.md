# 夯拉榜 · ROADMAP

> ⚠️ 本文件是项目的开发备忘录。用户明确要求：**多套视觉风格是必须交付的功能，后续版本要逐个补齐，不许忘。** 每次迭代先来这里勾掉一项。

## v1.1 已完成（2026-09-13）— 产品级 UI 打磨（依据用户提供的设计提示词，批判性采纳）

- [x] 数据模型 v4：项目条目升级为「名称 + 图片 + 一句话描述」，档位增加 emoji 图标与描述，榜单增加副标题
- [x] 清爽亮色主题 `clean` 设为默认（曜黑 `studio` 保留为第二主题），配色全面低饱和克制化
- [x] 项目卡重做：图 + 名 + hover 编辑/删除角标；描述走 title 提示
- [x] 「添加项目 / 编辑项目」弹窗（名称 + Logo 上传 + 一句话描述）
- [x] 「档位编辑」弹窗（名称 / 描述 / 图标 / 颜色 / 删除），点击档位名打开
- [x] 「分享你的排名」弹窗：自动生成分享卡片预览 + 复制链接 + 下载 PNG
- [x] 素材池升级为「项目库（未排名）」：搜索过滤、添加项目按钮、快速回车
- [x] 标题 + 副标题（编辑器 / 预览 / 导出三处同步）
- [x] Demo 榜单换为「2026 年 AI 大模型从夯到拉」（ChatGPT/Claude/Gemini/DeepSeek/豆包/Kimi/元宝/通义）
- [x] 移动端底部固定操作栏（添加项目 / 预览 / 分享），顶栏动作在窄屏收起
- [x] 批判性舍弃提示词中的：三栏布局（挤压排行榜画布）、条目标签字段、社交平台直发按钮、"灵感"页——保持"认真设计的现代 Web App，只是文案有梗"

## v1.0 已完成（2026-09-13）— 技术栈现代化改造

按用户推荐的组合完成整体迁移，旧 vanilla 版留在 `legacy/` 留档：

- [x] React 19 + TypeScript(strict) + Vite 6 + Tailwind CSS 4（@theme inline + CSS 变量主题）
- [x] Zustand 5 状态管理（persist → localStorage；v2 旧档自动迁移；v1.1 起结构版本 v4 带 migrate）
- [x] dnd-kit 拖拽：跨容器实时移动、档内排序、DragOverlay 弹性动画
  - 踩坑记录：多容器场景 `closestCorners` 会被 chip 抢走 `over`，改用 `pointerWithin` 优先 + `onDragEnd` 兜底
- [x] Motion 动画：路由切换、拖拽浮层、Toast、弹窗
- [x] shadcn/ui 风格组件：Button（cva）、Dialog（Radix）、ConfirmDialog
- [x] Lucide React 图标全套替换
- [x] html-to-image 导出 PNG（2x 高清）
  - 踩坑记录：导出节点挂在视口外（left:-9999px）会被裁剪成空图，导出前需临时移入视口
- [x] React Router 7（HashRouter）四页面：编辑器 `/`、模板库 `/templates`、我的排名 `/mine`、预览分享 `/preview` + `/r/:data`
- [x] vite-plugin-singlefile：`npm run build` 产出单文件 `dist/index.html`（双击即用、file:// 离线可用——用户要求保留单网页方便展示）
- [x] vercel.json：推 GitHub → Vercel 导入即可上线
- [x] 评级模式互切（从夯到拉/SSR/T0-T4/S~D）、档位 2×2 工具条、旧 `#d=` 分享链接兼容跳转

## 视觉风格（风格 × 日夜 两维度，4 风格 × 2 明暗 = 8 套全部完成 ✅）

> v1.3 重构：风格和日夜解耦为两个独立维度。**每种风格都有对应的白天和黑夜配色**；
> 日夜按钮只在当前风格内切换明暗（不会跳到别的风格），风格下拉只换风格（保持当前明暗）。

| 风格 | 白天 | 黑夜 | 个性 |
|---|---|---|---|
| **经典梗图** `tier` | ✅ | ✅ | **基础风格**：彩色横向色块 + 黑色粗体大字标签 + 扁平无渐变无阴影，经典 Tier List 梗图表格；配「从夯到拉」评级 = 红橙夯/橙黄顶级/明黄人上人/浅黄NPC/浅蓝拉完了 |
| 现代 `classic` | ✅ | ✅ | 白底/深底工作台，低饱和档位色阶，柔和渐变标签 |
| 抽象梗风 `hype` | ✅ | ✅ | 奶油纸/深可可底 + 玫红/亮黄撞色，暖调贴纸质感 |
| 赛博霓虹 `neon` | ✅ | ✅ | 冷白/深蓝黑底 + 青/品红霓虹双强调色，游戏榜气质 |

**经典梗图联动**：档位名为「夯顶级人上人NPC拉完了」时切入该风格，档位配色自动换成经典梗图色（项目与档位名保留）；切出到其它风格且当前配色为经典梗图预设时自动换回。
**实现**：`src/lib/themes.ts` 的 `THEMES` 以 `"style-mode"` 为键登记变体（`{ style, mode, scheme, vars, export }`）；`applyTheme(style, mode)` 同步 CSS 变量与 `color-scheme`；导出画布经 `exportPalette(style, mode)` 跟随；档位色属于评级预设（`presets.ts` 的 `tier` 预设），风格切换经 `applyPresetColors` 只换色不改名不动项目。

## v1.4 已完成（2026-09-13）— 经典梗图风格 + 字体/动效系统

- [x] **经典梗图风格**（tier-day/tier-night）：横向纯色色块 + 黑色粗体大字标签 + 清晰行边界，扁平无渐变；「夯」行红橙最醒目；与现代质感细节（圆角/间距）兼容
- [x] **档位配色联动**：切风格时档位名匹配「从夯到拉」模板则自动配对经典梗图配色（`applyPresetColors` 只换色，保留档位名与项目）
- [x] **等级标签字体系统**：标准（系统粗黑体）/ 艺术（得意黑 Smiley Sans，OFL 开源、斜体速度感）随时切换；字体文件内置（1.15MB woff2）离线可用；fallback 链得意黑→微软雅黑→苹方；`font-display: swap` + `font-synthesis: none`
- [x] **等级标签动效系统**：关闭/轻微/标准/强烈 四档；斜切滑入定格（skew 回正 + blur→clear + spring pop），无循环无闪烁；档位首次出现与项目拖入时各播一次；`prefers-reduced-motion` 自动关闭
- [x] 入口：顶栏「显示与样式」面板（等级标签字体 / 标签动效 / 项目标签样式 / 显隐开关 同面板）

## v1.3 已完成（2026-09-13）— 主题维度重构 + 显示设置

- [x] **主题系统重构**：`theme` 单字段拆为 `style`（风格）+ `mode`（day/night）两个独立维度，持久化 v4→v5 自动迁移（旧 clean/studio/hype/neon 值映射到新结构）
- [x] **日夜按钮只切明暗**：当前风格内 day ↔ night，不再跳风格（用户反馈：不允许"某风格一点按钮就变成别的风格"）
- [x] **风格下拉只换风格**：保持当前明暗，选经典/抽象梗风/赛博霓虹
- [x] **显示设置**（顶栏滑杆图标弹层，编辑器/预览/导出三处同步）：
  - 显示图片 / 显示名称 / 图片歪着放（按项目 id 稳定随机角度 ±tiltMax）
- [x] 图片放大：编辑器卡片 56px / 预览 48px / 导出 64px（v1.2 反馈）
- [x] FancySelect 自定义下拉：面板卡片质感、主题分组配色圆点、评级档位色条预览、当前项高亮、点击外部/Esc 关闭（v1.2 反馈）

## v1.3 已完成（2026-09-13）— 主题维度重构 + 显示设置

- [x] **主题系统重构**：`theme` 单字段拆为 `style`（风格）+ `mode`（day/night）两个独立维度，持久化 v4→v5 自动迁移（旧 clean/studio/hype/neon 值映射到新结构）
- [x] **日夜按钮只切明暗**：当前风格内 day ↔ night，不再跳风格（用户反馈：不允许"某风格一点按钮就变成别的风格"）
- [x] **风格下拉只换风格**：保持当前明暗，选经典/抽象梗风/赛博霓虹
- [x] **显示设置**（顶栏滑杆图标弹层，三个开关，编辑器/预览/导出三处同步）：
  - 显示图片：关掉后卡片只显示项目名称（图片数据保留）
  - 显示名称：关掉后带图项目只显示图片（纯 Logo 排名模式；无图项目始终显示名称防空白）
  - 图片歪着放：按项目 id 生成稳定随机角度（±6°）旋转，贴纸感，导出图同样生效
- [x] 图片放大：编辑器卡片 56px / 预览 48px / 导出 64px（v1.2 反馈）
- [x] FancySelect 自定义下拉：面板卡片质感、主题分组配色圆点、评级档位色条预览、当前项高亮、点击外部/Esc 关闭（v1.2 反馈）

## v1.2 修复（2026-09-13）— 用户实测反馈

- [x] **修复点击日夜按钮整页白屏**：根因是 `useBoard(...) === "studio" || useBoard(...) === "neon"` 的 `||` 短路导致 hook 调用数量随主题变化，违反 React Hooks 规则（React 19 未捕获错误会直接清空 DOM，表现为"跳转到空白页"）。修复为单次订阅 + selector 内计算。
- [x] **修复暗色主题下一片白**（v0.2 起潜伏）：`applyTheme` 变量名转换产出 `--c-panel2/--c-accent2`，而 CSS 定义为 `--c-panel-2/--c-accent-2`，名称不匹配导致这些变量永不跟随主题。已统一命名。
- [x] 增加 `color-scheme` 同步 + placeholder 全局主题色，原生控件（下拉/输入框/滚动条）在暗色下正常渲染。
- [x] 增加**独立于 React 树的错误浮层**：白屏类崩溃不再无信息，直接显示错误堆栈（main.tsx）。
- [x] 切换评级模式时提供「保留项目跟着切 / 清空到项目库 / 取消」三选项（PresetSwitchDialog，模板页同用）。

## 评级模式 / 模板计划

- [ ] 题材模板库（模板页占位卡已放）：游戏强度榜 / 热梗榜 / 追剧榜，一键填充档位 + 示例项目
- [ ] 星级模式（★5~★1）、音游难度模式
- [ ] "保存为自定义预设"：把当前档位结构存成新模板

## 形态计划

1. **解说排行榜视频**（用户勾选过、未做）：逐档揭晓动画（从拉完了倒序到夯）+ 字幕 + 配音。技术路线：canvas/DOM 逐帧动画 + `MediaRecorder`（`canvas.captureStream()`）导出 WebM；配音 Web Speech API 或上传音频。
2. **多人投票榜**：先做"导出/导入 JSON 合并"的轻方案，后续按需上后端。
3. **只读分享页增强**：点赞/踩、榜单评论（需后端）。

## 编辑器体验 backlog

- [ ] 撤销 / 重做（Ctrl+Z，Zustand 加 temporal 中间件即可）
- [ ] 档位拖拽排序（档位行整体 sortable，dnd-kit 同套方案）
- [ ] 移动端触摸拖拽实测与调优（PointerSensor 原生支持触屏）
- [ ] 导出选项（1x/3x、透明底、方形比例）
- [ ] IndexedDB 存图片（图片多了 localStorage 会吃紧）
- [ ] git init + 提交，形成版本历史

## 已知限制

- 分享链接把整个榜单（含图片 base64）塞进 URL，图多时链接很长——建议改用下载 PNG。
- 快照与自动保存在 localStorage（约 5MB 配额），图片多时优先用导出/分享留底；后续迁 IndexedDB。
- `dist/index.html` 单文件版在 `file://` 下功能完整，但剪贴板 API 个别浏览器有限制（已做 execCommand 降级）；正式部署 https 体验最佳。
